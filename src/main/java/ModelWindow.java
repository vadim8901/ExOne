import javax.swing.*;
import java.awt.*;

public class ModelWindow extends JFrame {
    public ModelWindow() {
        setTitle("Model");
        setBounds(300, 300, 600, 600);
        setVisible(true);
    }
}
